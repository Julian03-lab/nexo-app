import { Stack } from "expo-router";

const RecruiterLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
};

export default RecruiterLayout;
